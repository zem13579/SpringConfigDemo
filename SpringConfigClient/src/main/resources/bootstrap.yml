spring:
  application:
    name: svcA
  profiles:
    active: pro
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server

management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8086/config-server/eureka/

