
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        composite:
        - type: native
          search-locations: [ classpath:/svcA, classpath:/svcB ]
        bootstrap: true
  profiles:
    active: composite

eureka:
  instance:
    hostname: localhost
    instance-id: config-server
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/${spring.application.name}/eureka/
logging:
  level:
    root: debug